import React from 'react';
import PropTypes from 'prop-types';
import styles from './Cities.module.css';

const Cities = () => (
  <div className={styles.Cities} data-testid="Cities">
    Cities Component
  </div>
);

Cities.propTypes = {};

Cities.defaultProps = {};

export default Cities;
